import string
import random
def random_generate_password(leng=22):
    char=string.digits+string.punctuation+string.ascii_letters
    password = ''.join(random.choice(char)
    for i in range(leng))
    return password
print("generated password:",random_generate_password(22))
